# Rust 支持手动测试检查清单

## 前置条件
- [x] 插件已成功构建: `./gradlew buildPlugin`
- [x] CLI 已安装并可用
- [x] 测试数据文件已创建: `test_data/sample.rs`

## 测试步骤

### 1. 启动沙盒 IDE
```bash
./gradlew runIde
```

等待沙盒 IDE 启动...

### 2. 配置插件（如需要）
- [ ] 打开 Settings -> Tools -> CodeI18n
- [ ] 确认插件已启用
- [ ] 确认 CLI 路径正确（或在 PATH 中）

### 3. 测试 Rust 文件支持

#### 3.1 打开 Rust 文件
- [ ] 在沙盒 IDE 中，使用 File -> Open 打开项目根目录
- [ ] 导航到 `test_data/sample.rs`
- [ ] 打开文件

#### 3.2 验证文件识别
- [ ] 文件加载无错误
- [ ] 没有弹出错误对话框
- [ ] IDE 状态栏无错误提示

#### 3.3 查看日志
- [ ] Help -> Show Log in Finder (macOS) 或 Show Log in Explorer (Windows)
- [ ] 打开 `idea.log`
- [ ] 搜索 "Processing file: sample.rs"
- [ ] 验证日志中有记录（表示文件被识别）

**预期日志**:
```
INFO - CommentTranslationFoldingBuilder - Processing file: sample.rs
```

#### 3.4 验证翻译功能（如 CLI 已配置）
- [ ] 如果已配置翻译，注释应显示中文翻译
- [ ] 鼠标悬停在注释上应显示英文原文
- [ ] 点击折叠区域应展开/折叠

**注意**: 如果没有配置翻译或翻译数据为空，这是正常的。关键是文件被正确识别且没有错误。

### 4. 测试 Go 文件向后兼容性

#### 4.1 创建或打开 Go 文件
- [ ] 在沙盒 IDE 中创建新的 `.go` 文件或打开现有 Go 文件
- [ ] 添加一些英文注释

#### 4.2 验证 Go 支持仍然有效
- [ ] Go 文件加载无错误
- [ ] 翻译功能正常工作（如已配置）
- [ ] 没有回归问题

### 5. 测试混合项目

#### 5.1 项目中同时存在 Go 和 Rust 文件
- [ ] 在同一项目中打开 `.go` 和 `.rs` 文件
- [ ] 在文件间切换
- [ ] 验证两种文件类型都被正确处理

#### 5.2 验证缓存隔离
- [ ] 打开 `sample.rs`
- [ ] 打开 `.go` 文件
- [ ] 再次打开 `sample.rs`
- [ ] 验证没有缓存冲突（通过日志）

### 6. 边界情况测试

#### 6.1 空 Rust 文件
- [ ] 创建一个空的 `.rs` 文件
- [ ] 打开它
- [ ] 验证没有错误

#### 6.2 Rust 文件带各种注释类型
- [ ] `//` 单行注释
- [ ] `///` 文档注释
- [ ] `//!` 内部文档注释
- [ ] `/* */` 块注释
- [ ] `/** */` 文档块注释
- [ ] 验证所有类型都被识别（通过日志中的注释数量）

### 7. 性能测试

#### 7.1 创建大文件
```bash
# 生成包含 1000 个注释的 Rust 文件
for i in {1..1000}; do
  echo "// This is comment $i"
  echo "fn func$i() {}"
done > test_data/large.rs
```

#### 7.2 测试性能
- [ ] 打开 `test_data/large.rs`
- [ ] 文件加载时间 < 5 秒
- [ ] IDE 保持响应
- [ ] 没有 UI 冻结

### 8. 插件禁用测试

#### 8.1 禁用插件
- [ ] Settings -> Tools -> CodeI18n -> 取消选中 "Enable"
- [ ] 点击 Apply

#### 8.2 验证行为
- [ ] 打开 `.rs` 文件
- [ ] 验证没有翻译显示（正常）
- [ ] 验证没有错误

#### 8.3 重新启用
- [ ] 重新启用插件
- [ ] 验证翻译功能恢复

## 测试结果记录

### 通过的测试
- [ ] Rust 文件识别
- [ ] CLI 集成（日志验证）
- [ ] Go 文件向后兼容性
- [ ] 混合项目支持
- [ ] 边界情况处理
- [ ] 性能测试
- [ ] 插件开关功能

### 失败的测试
_记录任何失败的测试和错误消息_

## 完成标准

✅ **测试通过条件**:
- 所有核心测试场景通过
- 没有严重错误或异常
- Go 支持无回归
- 性能满足要求

❌ **如果失败**:
- 记录详细错误信息
- 附上日志文件片段
- 修复问题后重新测试

## 测试人员签名

- 测试日期: __________
- 测试人员: __________
- 测试版本: __________
- 测试结果: □ 通过 □ 失败 □ 部分通过

## 附加注释

_记录任何观察到的问题、建议或改进点_
